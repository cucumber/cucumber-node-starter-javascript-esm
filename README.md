# cucumber-node-starter-javascript-esm

Starter template for a [cucumber-node](https://github.com/cucumber/cucumber-node) project using ESM.

## Usage

Work on your feature files and support code in the `features` directory, and then run:

```shell
npm test
```